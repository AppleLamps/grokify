/** Canonical public URL for grokify.com (SEO, Open Graph, share links, API referers). */
export const SITE_URL = 'https://www.grokify.com';

export const SITE_NAME = 'Grokify';

/** Hostname without protocol — used in copy and CORS defaults. */
export const SITE_DOMAIN = 'www.grokify.com';
